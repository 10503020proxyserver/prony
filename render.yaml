services:
  - type: web
    name: 10503020-google-co-google-com-icloud
    env: python
    region: Oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn --port $PORT --host 0.0.0.0 偽造ページなし:app"
